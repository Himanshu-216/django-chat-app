server:
  http_listen_port: 3200

distributor:
  receivers:
    otlp:
      protocols:
        # grpc:
        #   endpoint: "0.0.0.0:4317"
        http:
          endpoint: "0.0.0.0:4318"

# ingester:
#   wal:
#     enabled: true
#     dir: /tmp/tempo/wal
#   shard_by_all_labels: true
#   max_concurrent_pushes: 16

storage:
  trace:
    backend: "local"
    local:
      path: "/var/lib/tempo"

stream_over_http_enabled: true
usage_report:
  reporting_enabled: true
